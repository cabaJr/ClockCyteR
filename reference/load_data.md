# load_data

creates Clean_sample_data object and stores into myCleanSample list

## Usage

``` r
load_data(env, vessel = "Front-Left")
```

## Arguments

- env:

  App_settings environment

- vessel:

  Vessel position when uploaded
